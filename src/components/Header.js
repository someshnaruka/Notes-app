import "../App.css";
import NotesIcon from '@mui/icons-material/Notes';

const Header=(props)=>{
    return (
        <header>
            <h1><NotesIcon />Keeper</h1>
        </header>
    );
};

export default Header;